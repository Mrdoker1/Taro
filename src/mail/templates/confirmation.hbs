<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Email Confirmation</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', system-ui, sans-serif;
            line-height: 1.5;
            color: #1f2937;
            background-color: #f9fafb;
            margin: 0;
            padding: 20px;
        }
        .email-wrapper {
            max-width: 480px;
            margin: 0 auto;
        }
        .container {
            background: #ffffff;
            border-radius: 8px;
            border: 1px solid #e5e7eb;
            overflow: hidden;
        }
        .header {
            padding: 32px 24px 0;
            text-align: center;
        }
        .app-name {
            font-size: 18px;
            font-weight: 600;
            color: #374151;
            margin-bottom: 8px;
        }
        .title {
            font-size: 24px;
            font-weight: 700;
            color: #111827;
            margin-bottom: 8px;
        }
        .subtitle {
            font-size: 14px;
            color: #6b7280;
            margin-bottom: 0;
        }
        .content {
            padding: 24px;
        }
        .greeting {
            font-size: 16px;
            margin-bottom: 16px;
            color: #374151;
        }
        .message {
            font-size: 14px;
            line-height: 1.6;
            color: #4b5563;
            margin-bottom: 24px;
        }
        .button-container {
            text-align: center;
            margin: 32px 0;
        }
        .confirm-button {
            display: inline-block;
            background-color: #8b5cf6;
            color: #ffffff !important;
            padding: 12px 32px;
            text-decoration: none;
            border-radius: 6px;
            font-weight: 500;
            font-size: 14px;
            border: none;
            cursor: pointer;
            transition: all 0.2s ease;
        }
        .confirm-button:hover {
            background-color: #7c3aed;
            color: #ffffff !important;
        }
        .confirm-button:visited {
            color: #ffffff !important;
        }
        .confirm-button:active {
            color: #ffffff !important;
        }
        .info-box {
            background-color: #faf5ff;
            border-left: 4px solid #8b5cf6;
            padding: 16px;
            margin: 24px 0;
            border-radius: 0 6px 6px 0;
        }
        .info-title {
            font-weight: 600;
            color: #6b21a8;
            margin-bottom: 4px;
            font-size: 14px;
        }
        .info-text {
            color: #7c3aed;
            font-size: 13px;
        }
        .link-fallback {
            margin: 24px 0;
            padding: 16px;
            background-color: #f9fafb;
            border-radius: 6px;
        }
        .link-text {
            font-size: 12px;
            color: #6b7280;
            margin-bottom: 8px;
        }
        .link-url {
            word-break: break-all;
            color: #4b5563;
            font-family: ui-monospace, 'SF Mono', Monaco, 'Cascadia Code', 'Roboto Mono', monospace;
            font-size: 12px;
            background-color: #ffffff;
            padding: 8px;
            border-radius: 4px;
            border: 1px solid #e5e7eb;
        }
        .footer {
            background-color: #f9fafb;
            padding: 24px;
            text-align: center;
            border-top: 1px solid #e5e7eb;
        }
        .footer-text {
            font-size: 12px;
            color: #6b7280;
            margin: 0;
        }
        @media (max-width: 500px) {
            .email-wrapper {
                margin: 0;
                padding: 0;
            }
            .container {
                border-radius: 0;
                border-left: none;
                border-right: none;
            }
            .content {
                padding: 20px;
            }
            .header {
                padding: 24px 20px 0;
            }
        }
    </style>
</head>
<body>
    <div class="email-wrapper">
        <div class="container">
            <div class="header">
                <div class="app-name">Seluna App</div>
                <h1 class="title">Email Confirmation</h1>
                <p class="subtitle">Account activation</p>
            </div>
            
            <div class="content">
                <p class="greeting">Welcome!</p>
                
                <p class="message">
                    Thank you for signing up for Seluna App. 
                    To complete the activation of your account, please 
                    click the button below to confirm your email address.
                </p>
                
                <div class="button-container">
                    <a href="{{confirmationUrl}}" class="confirm-button">Confirm Account</a>
                </div>
                
                <div class="info-box">
                    <div class="info-title">Good to know</div>
                    <div class="info-text">
                        After confirmation, you will have full access to all app features.
                    </div>
                </div>
                
                <div class="link-fallback">
                    <p class="link-text">If the button doesn't work, copy this link:</p>
                    <div class="link-url">{{confirmationUrl}}</div>
                </div>
            </div>
            
            <div class="footer">
                <p class="footer-text">Seluna App Team</p>
            </div>
        </div>
    </div>
</body>
</html>
