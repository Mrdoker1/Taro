# Course Editor Setup - React + Mantine

## 🎉 Новый редактор курсов готов!

Редактор полностью переписан на React с использованием Mantine UI библиотеки.

## 📦 Установка

Зависимости уже установлены в `course-editor-app/`

## 🚀 Запуск в режиме разработки

### Вариант 1: Запуск всего проекта одной командой (рекомендуется)

```bash
npm start
```

Эта команда запустит:
- 🔷 **API** (NestJS) на порту 3000
- 🔶 **EDITOR** (React) на порту 3001

### Вариант 2: Запуск по отдельности

```bash
# Терминал 1: Запуск NestJS бэкенда
npm run start:dev

# Терминал 2: Запуск React редактора
npm run start:editor
```

### Вариант 3: Запуск только редактора

```bash
# Из корневой директории
cd course-editor-app
npm run dev
```

Редактор будет доступен на: **http://localhost:3001**

## 🏗️ Сборка для продакшена

```bash
npm run build:editor
```

Собранные файлы попадут в `src/course-editor/public/`

После сборки NestJS будет автоматически отдавать React приложение на `/course-editor`

## 🔐 Авторизация

- **Username:** `admin`
- **Password:** `tarot2024`

## ✨ Возможности

### Основные
- ✅ Создание, редактирование и удаление курсов
- ✅ Поддержка двух языков (RU/EN)
- ✅ Редактирование глав, страниц и блоков контента
- ✅ Поддержка разных типов блоков: Markdown, Image, Video, Card

### Markdown редактор
- ✅ Синтаксис Markdown
- ✅ Превью в реальном времени для каждого блока
- ✅ Удобный монопространственный шрифт

### Превью курса
- ✅ Полноценное модальное окно превью
- ✅ Навигация по главам и страницам
- ✅ Переключение языков
- ✅ Рендеринг всех типов блоков

### UI/UX
- ✅ Темная тема в стиле Seluna (золотой + темно-фиолетовый)
- ✅ Современные Mantine компоненты
- ✅ Адаптивный дизайн
- ✅ Уведомления об успехе/ошибках
- ✅ Плавные анимации

## 📁 Структура проекта

```
course-editor-app/
├── src/
│   ├── components/
│   │   ├── Login.jsx           # Страница авторизации
│   │   ├── CourseList.jsx      # Список курсов в сайдбаре
│   │   ├── CourseEditor.jsx    # Главный редактор курса
│   │   ├── ChapterEditor.jsx   # Редактор главы
│   │   ├── PageEditor.jsx      # Редактор страницы
│   │   ├── BlockEditor.jsx     # Редактор блока с превью
│   │   └── Preview.jsx         # Модальное окно превью
│   ├── api/
│   │   └── client.js           # API клиент (axios)
│   ├── types/
│   │   └── course.ts           # TypeScript типы
│   ├── theme/
│   │   └── theme.js            # Mantine тема Seluna
│   ├── App.jsx                 # Главный компонент
│   └── main.jsx                # Точка входа
├── index.html
├── vite.config.js
└── package.json
```

## 🎨 Тема Seluna

Цветовая палитра:
- **Основной золотой:** `#E8D28C`
- **Темный фон:** `#0E0B1D`
- **Сайдбар:** `#1A1625`
- **Акценты:** Фиолетовый для Preview кнопки

## 🔄 API Endpoints

Все endpoints остались прежними:
- `POST /course-editor/login` - Авторизация
- `GET /course-editor/api/courses` - Список курсов
- `GET /course-editor/api/courses/:slug` - Получить курс
- `PUT /course-editor/api/courses/:slug` - Сохранить курс
- `POST /course-editor/api/courses` - Создать курс
- `DELETE /course-editor/api/courses/:slug` - Удалить курс

## 🐛 Troubleshooting

### Порт 3001 занят
```bash
# Измените порт в vite.config.js
server: {
  port: 3002, // Ваш порт
}
```

### Ошибки CORS
Убедитесь, что NestJS бэкенд запущен на порту 3000

### Не работает авторизация
Проверьте, что токен сохраняется в localStorage и добавляется в заголовки запросов

## 📝 Следующие шаги

1. Запустите бэкенд: `npm run start:dev`
2. Запустите редактор: `npm run start:editor`
3. Откройте http://localhost:3001
4. Войдите с учетными данными
5. Создавайте и редактируйте курсы!

## 🎯 Что изменилось

### Удалено
- ❌ Старый HTML редактор (`editor.html`)
- ❌ Vanilla JavaScript код
- ❌ Кастомные CSS стили

### Добавлено
- ✅ React + Vite
- ✅ Mantine UI компоненты
- ✅ TypeScript типы
- ✅ Модульная структура
- ✅ Современный стек разработки

Приятной работы! 🚀
